# DESIGN.md

> Fund CRM Demo 设计说明  
> 日期：2026-04-06  
> 说明：技术版本与依赖以仓库根目录 `package.json` 为准。

---

## 1. 需求分析

### 1.1 我如何理解这个场景

这个项目不是面向散户的营销页面，而是资管公司或渠道销售人员日常使用的内部 CRM。核心任务不是“展示品牌”，而是帮助销售更快完成以下几件事：

- 了解当前在售基金的关键信息
- 维护客户关系，记录每次沟通和跟进
- 查询“某个客户持有哪些产品”或“某个产品被哪些客户持有”
- 通过 Dashboard 快速了解业务节奏

从使用场景看，销售最关心的是“找得到、看得快、记得下、能跟进”。所以我把项目重点放在产品、客户、持仓、跟进这几类核心数据的联动上，而不是堆很多边缘功能。

### 1.2 我做了哪些功能，为什么

- **Dashboard 数据概览**  
  用于快速查看业务整体情况，包括核心指标、基金分布、客户风险分布、近期跟进趋势。这样用户进入系统后可以先看全局，再决定接下来关注哪个客户或产品。

- **基金模块**  
  提供基金列表、筛选、排序、详情查看。列表支持搜索、风险等级和状态筛选；详情既支持独立页面，也支持 Drawer 快速预览。这样做是为了兼顾“列表中快速浏览”和“进入详情深入查看”两种路径。

- **客户模块**  
  提供客户列表和客户详情，详情里能看到客户基本信息、持仓信息和跟进记录。这个模块的重点是让销售能快速判断客户画像，并把客户与产品、跟进串起来。

- **跟进模块**  
  提供全局跟进记录列表，以及新增跟进的能力。这样既能从客户详情进入时间线，也能从全局视角回看最近的销售动作，满足 CRM 对“节奏管理”的需求。

- **双向查询能力**  
  通过持仓数据把客户和基金关联起来，支持“某客户买了什么”和“某产品被谁持有”两种查询方向。这是题目里最明确的业务点之一，所以我优先保证这部分结构清晰。

- **Agent 能力**  
  我没有做一个泛聊天机器人，而是把 Agent 定位为业务辅助入口，主要用于自然语言查询和辅助录入。这比单纯放一个聊天框更贴近基金销售场景。

### 1.3 有什么重要但时间有限没做

- 自动化测试还不够，尤其是筛选、详情联动、URL 状态同步这些关键链路
- 权限体系没有实现，例如按销售归属过滤客户和跟进数据
- Agent 目前还是规则驱动，复杂口语、多轮澄清和容错能力有限
- 响应式只做到了基本可用，桌面场景优先，移动端没有专门打磨

### 1.4 如果再给我 2 天，我会优先做什么

1. 补关键链路测试，优先覆盖基金列表筛选、详情打开、跟进新增这几条主流程
2. 做 Agent 与页面的闭环，例如从查询结果直接跳转客户详情或打开基金详情
3. 增加更贴近真实企业系统的权限和数据归属逻辑，例如只看自己负责的客户

---

## 2. 技术选型

### 2.1 技术方案

| 类别 | 选型 | 选择理由 |
|------|------|----------|
| UI 框架 | **Ant Design 6.x** | 企业内部系统常见方案，`Table`、`Form`、`Drawer`、`Modal` 等组件成熟，开发效率高 |
| 路由 | **React Router v7** | 适合 SPA 路由管理，能够清晰组织基金、客户、跟进、Dashboard、Agent 等页面 |
| Mock API | **MSW 2.x** | 拦截真实 HTTP 请求，保证数据仍然通过异步接口获取，且接口组织方式和真实后端一致 |
| 图表 | **Recharts** | 轻量、React 原生，足够支撑 Dashboard 中的趋势图、分布图、统计图 |
| Agent | **纯前端意图解析引擎** | 无需 API Key，适合demo场景，可实现自然语言查询和辅助录入；后续可扩展接入真实模型 |

### 2.2 为什么选这些，而不是其他选项

- **Ant Design 而不是自己搭组件**  
  这个项目重点在业务信息结构和交互闭环，不在于从零设计一套组件库。Ant Design 对中后台场景支持非常成熟，能让我把时间放在业务逻辑而不是基础 UI 上。

- **React Router 而不是更重的全栈框架**  
  这是一个标准的前端单页应用，主要需求是页面路由和状态切换，不依赖 SSR 或 SEO。相比引入更重的框架，React Router 更直接、更符合题目目标。

- **MSW 而不是直接在组件里写假数据**  
  题目明确要求数据异步获取，不能在组件里直接 import 数据。MSW 能让前端像请求真实后端一样工作，比简单的本地假数据更贴近真实工程实践。

- **Recharts 而不是更复杂的图表方案**  
  当前需求主要是常规的统计图，不是复杂大屏或重交互可视化。Recharts 对这个规模的项目更合适，接入成本低，表达也足够清晰。

- **纯前端 Agent 而不是直接接 LLM API**  
  demo项目更看重思路和落地，而不是 API 调用本身。纯前端意图解析能在没有后端和 API Key 的情况下稳定演示业务价值，也更容易讲清楚设计取舍。

### 2.3 简要架构图

```text
┌──────────────────────┐
│   Pages / Components │
└──────────┬───────────┘
           │
           ▼
┌──────────────────────┐
│      API Layer       │
│  clientApi / fundApi │
└──────────┬───────────┘
           │
           ▼
┌──────────────────────┐
│     MSW Handlers     │
└──────────┬───────────┘
           │
           ▼
┌──────────────────────┐
│     Mocked Data      │
│ clients / funds /    │
│ holdings / followups │
└──────────────────────┘
```

这个结构的目的是让页面层只依赖 API，不直接依赖 mock 数据。这样如果后续切到真实后端，主要替换的是 API 层和 mock 层，而不是大面积改页面组件。

---

## 3. AI 协作日志

### 3.1 我在哪些环节使用了 AI

这个项目中我主要在以下环节使用了 AI：

- 项目启动阶段：先在 Cursor 中统一上下文，开启 Plan 模式，让 AI 先理解业务场景、交付边界和技术原则
- 方案设计阶段：用 AI 帮我拆解模块、梳理数据关系、评估哪些功能更适合 4 天内交付
- 文档整理阶段：辅助整理技术选型、需求分析和后续复盘表达

我对 AI 的使用原则是：先让它帮助我统一问题空间和实现路径，再决定哪些建议真正落进代码，而不是直接把生成结果原样照搬。

### 3.2 具体案例 1：项目启动时的总控 Prompt

项目一开始，我先在 Cursor 中写了一条总控 Prompt，用来统一整个项目的背景、目标和输出方式，并开启 Plan 模式。目的不是立刻生成代码，而是先让 AI 和我站在同一个上下文里。

**Prompt**

```text
你是一个资深全栈工程师 + 前端架构师 + 企业后台产品设计顾问。
我要做一个“资管公司内部 Web 工具”的前端/全栈demo项目，服务对象是渠道销售人员，核心目标是：

1. 快速了解公司在售基金产品的关键信息
2. 维护客户关系，记录客户跟进情况
3. 快速查询“某个客户买了哪些产品”或“某个产品被哪些客户持有”
4. 做一个有业务价值的数据概览 Dashboard
5. 尽量加入一个与业务深度结合的 Agent 能力，而不是简单聊天框

要求：
- 必须使用 React
- 允许自由选择其他技术栈
- 数据必须是异步获取，不能在组件中直接 import 数据
- 至少 8 个产品 + 8 位客户

我希望你输出内容时遵循这些原则：
- 优先做业务合理、信息结构清晰、代码可维护的方案
- 不要盲目堆功能
- 优先考虑企业内部系统常见模式：列表、筛选、详情、表单、图表、边界状态
- 每次输出都要说明“为什么这样设计”
- 给代码时默认使用 TypeScript
- 如果我让你写页面，请优先考虑可扩展的组件拆分、状态流、类型定义和模拟接口层
- 如果涉及 Agent，请优先考虑“自然语言辅助录入跟进记录”或“自然语言查询业务数据”这类强业务结合场景
- 避免过度工程化，但保持专业度

当我要求你生成代码时：
- 先给出文件结构和职责划分
- 再输出核心代码
- 尽量避免把所有逻辑塞进一个文件
- 给出必要的 mock api / types / store / hooks / ui 组件拆分建议
- 默认采用适合 4 天内完成、可部署演示的实现方案。
```

**AI 输出**

这轮输出内容比较多，这里不逐段展开，只保留对后续实现最有影响的部分。基于这条 Prompt，AI 先给出的不是零散代码，而是项目整体的实现方向，包括：

- 技术栈的初步选型
- 核心数据模型的划分
- 推荐的目录与文件结构
- 基金、客户、跟进、Dashboard、Agent 等模块拆分
- 适合 4 天左右完成的开发节奏

这一步的价值主要是统一上下文，让后续每一轮设计都围绕同一个项目目标展开。

**我的采纳 / 修改 / 放弃**

- **采纳**
  我采纳了这条总控 Prompt 的核心作用：先统一背景和边界，再展开具体实现。后续项目里的功能边界、模块划分和文档写法，基本都沿着这条 Prompt 定下来的方向推进。

- **修改**
  AI 给出的方向是原则性的，我在落地时仍然做了实际约束下的调整。例如最终技术栈以仓库实际依赖为准，UI 采用了 Ant Design 6.x，路由用 React Router v7，Mock 采用 MSW 2.x，这些都以真实实现为准，而不是机械照搬任何一轮建议。

- **放弃**
  我没有把 AI 当成“自动产出完整方案”的工具，也没有直接采用任何可能导致过度工程化的建议，例如过早引入复杂全局状态、权限系统或完整 LLM 后端。对这个 Demo 项目来说，我更关注交付闭环和业务合理性。

### 3.3 具体案例 2：用 AI 做数据建模与类型定义

在统一上下文之后，我没有马上让 AI 写页面，而是先让它设计实体模型，再直接生成 TypeScript 类型。原因是这个项目的核心不是某个单独页面，而是“基金、客户、持仓、跟进”之间的数据关系。如果模型不稳，后面的列表、详情、Dashboard 和 Agent 都会松散；如果类型不统一，后续页面和 Mock 也容易各写各的。

**Prompt A**

```text
请为这个“基金产品 + 客户关系管理”项目设计一套合理的数据模型。 

至少包含这些实体：
1. Product 基金产品
2. Customer 客户
3. Holding 客户持仓关系
4. FollowUp 跟进记录

请输出：
1. 每个实体的字段定义
2. 字段类型
3. 字段业务含义
4. 实体之间的关系
5. 哪些字段适合展示在列表页，哪些适合放详情页
6. 哪些字段对 Dashboard 有价值
7. 哪些字段适合被 Agent 自动解析填充

要求：
- 字段不要只停留在“演示级假数据”，要有一定业务合理性
- 但不要复杂到像真实资管核心系统
- 要足以支撑搜索、筛选、持仓关系查询、跟进记录、Dashboard、Agent 辅助录入。
```

**AI 输出**

AI 返回的是一套比较完整的数据建模思路，而不是只给几个简单字段。核心内容包括：

- 以 `Product`、`Customer`、`Holding`、`FollowUp` 作为四个核心实体
- 用 `Holding` 连接客户和基金，承担“谁持有什么”的关系查询
- 用 `FollowUp` 表达销售沟通记录，并允许跟产品产生关联
- 区分哪些字段适合列表页，哪些更适合详情页
- 额外标出哪些字段适合用于 Dashboard 聚合，哪些字段适合 Agent 做自然语言解析

**Prompt B**

```text
基于你刚才设计的数据模型，请直接输出 TypeScript 类型定义。

要求：
- 使用 type 或 interface 都可以，但风格统一
- 枚举类字段请抽成联合类型
- 类型命名清晰，适合企业项目
- 至少包含：
  - Product
  - ProductType
  - ProductStatus
  - Customer
  - CustomerLevel
  - Holding
  - FollowUp
  - FollowUpMethod
  - FollowUpIntent
  - DashboardSummary
- 如果你认为有必要，可以补充用于表单和接口层的 DTO 类型
```

**AI 输出**

在 Prompt A 的基础上，AI 进一步把抽象模型落成了可以直接进入前端工程的类型定义。输出重点包括：

- 把产品状态、产品类型、客户等级、跟进方式、跟进意向等字段抽成联合类型
- 把 `Product`、`Customer`、`Holding`、`FollowUp` 这些核心实体整理成统一的 TypeScript 类型
- 补充了适合 Dashboard 聚合展示的汇总类型，以及后续表单或接口层可能会用到的 DTO 思路

**我的采纳 / 修改 / 放弃**

- **采纳**
  我采纳了四个核心实体的划分方式，也采纳了“客户和产品通过持仓关系连接”“跟进记录可以关联产品”这两个关键设计。同时我也采纳了“先把关键枚举和核心实体固化成 TypeScript 类型”的做法，让项目从一开始就围绕统一类型展开，而不是边写页面边补类型，一个好的产品，他的模型设计一定是优秀的，符合业务的，打好地基（但其实还是有些不符合业务，这个专门写了提示词在3.5）。

- **修改**
  AI 给出的模型和类型都偏完整，我在落地时做了收敛。保留了风险偏好、客户等级、最近跟进时间、产品风险等级等更贴近展示的字段；同时把类型命名、字段粒度和前端展示方式对齐到当前项目的真实实现里。

- **放弃**
  我没有引入过重的资管核心系统字段，也没有把类型体系扩展到过细的交易、清算、审批、账户体系。它们可能更“像真实系统”，但对于这个demo项目来说会明显增加复杂度，反而削弱完成度。

### 3.4 具体案例 3：生成并迭代 8+8 的 Mock 数据

在模型和类型确定之后，我继续使用 AI 生成演示用数据。这里的目标不是“凑够数量”，而是让数据足够支撑搜索、筛选、图表和 Agent 演示。

**Prompt C**

```text
请基于这套数据模型，生成一组适合演示的 mock 数据。

要求：
- 至少 8 个基金产品
- 至少 8 位客户
- 每位客户要有合理的持仓关系和跟进记录
- 数据分布要能支撑搜索、筛选、图表展示和 Agent 演示
- 产品类型、状态、规模、净值不要都一样，要有差异
- 客户画像尽量体现销售管理场景，比如风险偏好、客户等级、地区、最近跟进时间等
- 跟进记录要写得像真实销售记录，简洁但自然
- 输出格式：
  1. 先给出数据设计说明
  2. 再给出可直接放进 mock 文件中的 JSON / TS 数组
```

**AI 输出**

AI 给出的结果不只是“凑数量”，而是试图让数据更适合演示：

- 基金产品在类型、风险、状态、规模等维度上有区分
- 客户之间有不同的画像特征，例如地区、风险偏好、客户等级、最近跟进时间
- 持仓关系不是平均分配，而是能支撑“客户持有哪些产品”“某产品被哪些客户持有”的查询
- 跟进记录采用更接近销售日常表达的自然语言，而不是纯机器式占位文案

**我对 AI 生成数据的人工复核**

AI 生成 Mock 数据之后，我没有直接全量采用，而是按照题目要求和演示目标逐条复核了一遍，确认哪些地方已经满足，哪些地方还需要手动修正。

| 要求 | 当前状态 | 问题 |
|------|----------|------|
| `>=8` 个基金产品 | `✅ 12 个` | — |
| `>=8` 位客户 | `✅ 10 位` | — |
| 每位客户有持仓 + 跟进记录 | `⚠️ 部分满足` | `c004`、`c005`、`c007`、`c008`、`c009`、`c010` 各只有 1 条跟进，不足以体现“销售管理”场景 |
| 数据分布支撑筛选 / 图表 / Agent | `✅ 基本满足` | — |
| 产品类型 / 状态 / 规模 / 净值有差异 | `⚠️ 部分满足` | `募集中` 状态在类型定义里存在，但生成数据里没有实际用上 |
| 客户画像体现地区、客户等级等信息 | `✅ 基本满足` | 地区、客户等级、风险偏好等字段已覆盖，能够支撑基础演示 |

**我的采纳 / 修改 / 放弃**

- **采纳**
  我采纳了 AI 关于“数据必须有分布差异”的建议，没有让所有基金和客户都长得很像。这样 Dashboard 才有图可画，筛选和排序也才有意义。

- **修改**
  我会把 AI 生成的数据再手动收敛和校正。例如，部分客户虽然已经有跟进记录，但跟进条数不足以支撑“销售管理”场景；另外还需要确保字段命名、取值范围和项目类型定义一致，并补齐少量缺失状态，让数据更贴近基金销售 CRM，而不是普通联系人管理系统。

- **放弃**
  对于一些明显为了凑数量而产生、但对演示没有帮助的数据，我不会全盘接收。我的目标不是让数据越多越好，而是让每条数据都能服务于筛选、详情、图表或 Agent 演示。

### 3.5 具体案例 4：让 AI 审查模型是否足够“业务化”

在模型和 Mock 数据初步有了以后，我又专门让 AI 从“资深产品与后端”的角度审查这套设计，看看它是否真的像一个基金销售管理系统，而不只是普通 CRM 换了几个金融名词。

**Prompt D**

```text
请你站在资深产品与后端视角，审查我当前这套数据模型。 

请从以下角度给出反馈：
1. 是否足以体现“基金销售管理”业务理解
2. 是否只是普通 CRM 套壳
3. 哪些字段最容易被认为缺乏业务感
4. 哪些字段可以小幅增加，但显著提升专业度
5. 哪些字段其实没必要，会让项目显得臃肿
6. 如果你是资深产品与后端，这套模型哪里还不够成熟 
```

**AI 输出**

AI 在这一轮更像评审者，而不是生成器。它会指出：

- 当前模型哪些地方已经体现了基金销售场景
- 哪些字段仍然容易让项目看起来像“普通 CRM 改名版”
- 哪些字段可以少量增加，但能显著增强专业感
- 哪些设计如果继续往下加，会让项目显得臃肿或脱离边界

**AI 输出节选**

**1. 是否足以体现“基金销售管理”业务理解**

总体评价是“及格，但不出彩”。

AI 认为当前模型的加分项是真实的，例如选择了知名基金经理、使用了真实基金代码、收益字段里既有正数也有负数，跟进内容里也出现了“定投”“季报”“配置方案”等业务术语。这些都说明我做过基础调研，而不是只写了一套空泛的后台模型。

但它也指出，这些更多停留在“我知道基金是什么”的层面，还没有真正进入“我知道基金销售怎么管”的层面。最核心的缺失是合规维度，尤其是投资者适当性评估。在中国基金销售场景里，这是强监管要求，如果完全没有相关字段，模型就会显得业务感不够扎实。

**2. 是否只是普通 CRM 套壳**

AI 的判断比较直接：目前这套模型本质上仍然偏“通用 CRM 套壳”。如果把 `Fund` 换成普通 `Product`，把部分风险字段换成通用分类字段，整体结构依然能成立，还没有完全体现出基金销售 CRM 与普通客户管理系统之间的本质差异。

它给出的一个很有启发的判断是：真正能把“基金销售 CRM”和“通用 CRM”区分开的，不是页面长什么样，而是数据模型里有没有基金销售独有的业务概念。

| 业务概念 | 通用 CRM | 基金销售 CRM |
|----------|----------|----------------|
| 客户分类 | 大 / 中 / 小客户 | 普通投资者 vs 专业投资者 |
| 产品适配 | 客户偏好 | 适当性匹配，客户风险等级应覆盖产品风险等级 |
| 销售行为 | 联系记录 | 申购 / 赎回流水、销售留痕 |
| 盈亏表达 | 通常没有 | 基于持仓成本、净值和份额计算出的浮盈浮亏 |

**我的采纳 / 修改 / 放弃**

- **采纳**
  我采纳了“业务化不等于字段越多越好”的判断。对demo项目来说，最重要的是能看出我理解基金销售和客户维护的业务逻辑，而不是试图复制完整金融核心系统。

- **修改**
  AI 给出的“增强专业度”建议，我会只选择一小部分真正高价值的内容吸收进来，例如更能体现风险、适当性、跟进闭环或产品经营信息的字段，而不是全部加入。

- **放弃**
  对于会明显推高复杂度、但对页面体验和演示价值帮助有限的建议，我会主动放弃。这个阶段 AI 更像帮助我做“减法判断”，而不是继续往模型里堆东西。

### 3.6 具体案例 5：用 AI 做页面体验复盘

页面主体搭建完成之后，我没有再让 AI 生成整页代码，而是把它当成一次“设计评审 + 前端复盘”工具来使用，重点检查页面体验上还缺什么，以及哪些小改动最值得在有限时间里补上。

**Prompt E**

```text
请你站在高级产品设计师 + 前端工程师视角，帮我检查这个后台项目的页面体验还缺什么。

请重点看：
- 信息层级是否清晰
- 企业后台感是否足够
- 列表、筛选、详情的操作路径是否顺畅
- 有没有缺少 loading / empty / error 状态
- 有没有缺少让人觉得“高级”的小细节
- 哪些小改动成本低，但最能提升完成度

请输出“高性价比优化清单”，按优先级排序，每条都写清：
- 改什么
- 为什么
- 预估工作量
- 是否值得在 4 天项目里做
```

**AI 输出**

这轮 AI 输出的重点不是“大改界面”，而是一份偏体验审查的高性价比优化清单。它更关注：

- 当前页面的信息层级是否清楚，用户第一眼能不能抓住重点
- 列表、筛选、详情之间的操作路径是否顺畅
- loading / empty / error 三种状态是否补齐
- 是否缺少一些成本不高、但能明显提升完成度的细节

这类输出对我帮助比较实际，因为它不会把项目往“大改视觉风格”方向带，而是更贴近demo场景里的最后一轮体验打磨。

**我的采纳 / 修改 / 放弃**

- **采纳**
  我采纳了“优先做低成本高收益优化”的思路。对这个项目来说，页面体验的提升不一定来自重做设计，而往往来自状态补齐、路径顺滑和少量细节打磨。

- **修改**
  AI 给出的建议我不会逐项照做，而是会结合交付时间筛选。比如我会优先处理筛选区可用性、列表到详情的操作路径和三态反馈这类直接影响体验的问题；而像新增抽屉层来承载部分详情这类会增加交互层级、但收益有限的建议，我会根据现有页面结构谨慎取舍。

- **放弃**
  对于需要大改页面结构、重做视觉体系，或者明显超出 4 天交付边界的建议，我不会继续展开。这个阶段 AI 对我最大的价值是帮助做最后一轮体验复盘，而不是推动一次大范围返工。

### 3.7 AI 在这个项目里最帮得上忙和最帮不上忙的地方

- **最帮得上忙的地方**
  是在项目初期帮助我统一上下文、拆解问题、明确优先级，并在数据建模阶段快速提供多个候选方向。对这类demo项目来说，AI 很适合用来先搭出方案骨架，再由我结合业务目标、时间约束和实际实现情况做收敛与取舍。

- **最帮不上忙的地方**
  是替代业务判断和最终决策。比如模型应该控制在什么复杂度、哪些字段值得保留、哪些功能虽然能做但不值得在Demo里投入过多时间，这些仍然需要我结合题目要求、交付节奏和最终呈现效果自己判断。

---

## 4. 自我复盘

### 4.1 当前实现中已知但来不及修的问题

- 自动化测试不足，目前主要依赖手工验证
- Agent 的理解能力仍偏规则化，复杂表达处理有限
- 权限、登录、审计等企业后台常见能力没有实现
- 当前更偏演示型数据流，离真实生产环境还有距离
- 当前页面状态主要依赖 `useState` 和页面内局部状态管理。考虑到这是Demo，我没有额外引入全局状态库，而是优先把时间投入到业务流程、页面闭环和核心交互上。

### 4.2 如果这是真实企业项目，我会做哪些不同决策

- **按状态类型选择更合适的状态管理方案**  
  如果项目继续演进、跨页面共享状态变多，我会引入更明确的状态管理方案。客户端全局状态可以考虑 `Zustand`，从而让工程边界更清晰、维护成本更可控。

- **补全测试体系**  
  如果这是企业项目，我会优先为最重要的业务流程补自动化测试，例如列表筛选、详情查看、跟进新增等，避免后续迭代时把原本可用的功能改坏。

- **更早设计权限与数据归属**  
  在真实 CRM 中，销售通常只能看到自己负责的客户和跟进记录，因此权限模型会更早进入设计，而不是等到后期补。

- **补齐登录态与路由拦截**  
  如果这是企业项目，我会更早把登录态、路由拦截和无权限页面纳入整体设计。例如未登录时统一跳转登录页，登录态失效时自动回收会话，进入受限页面时给出明确的权限反馈，而不是把这些逻辑分散在各个页面里临时处理。

- **统一请求层，采用 Axios 封装**  
  如果这是企业项目，我会进一步采用统一的 `Axios` 封装，把 `baseURL`、超时、鉴权头、错误码处理、请求/响应拦截器等能力收敛到同一层，避免请求逻辑散落在各个业务模块里。
